# Test artifacts (quacker File-storage tests use t.TempDir(), this is a belt)
*.db
*.db-wal
*.db-shm
*.quacker.lock
*.test

# Example output (examples/dagsvg writes this to its working directory)
/dag.svg

# WIP website: a separate module/app, kept out of the library's build and CI
/website/

# macOS Finder metadata
.DS_Store

# Agent/tool session state
.zcode/
